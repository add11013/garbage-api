import { OpenAPIV3, OpenApiRequest, RequestValidatorOptions } from '../framework/types';
export declare class RequestValidator {
    private middlewareCache;
    private apiDoc;
    private requestOpts;
    private options;
    private validationModule;
    private ajv;
    constructor(apiDoc: OpenAPIV3.Document, options?: RequestValidatorOptions);
    validate(req: OpenApiRequest): void;
    compile(filepath: string): Promise<void>;
    loadCompiled(filepath: string): Promise<void>;
    private buildMiddlewareFromModule;
    private buildMiddleware;
}
